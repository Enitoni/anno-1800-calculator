export type Theme = {
  colors: {
    background: string
    primary: string
    accent: string
    overlay: string
  }
  fontColors: {
    normal: string
    muted: string
  }
  transparencies: {
    lightPositive: string
    strongPositive: string
    lightNegative: string
    strongNegative: string
  }
}
